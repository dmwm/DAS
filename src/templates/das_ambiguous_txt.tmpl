## this template is in text mode. no sanitization needed for $msg
#assert (isinstance($msg, str) or isinstance($msg, unicode))
DAS is unable to interpret your query:
$msg

For easier query debuging, please first use the DAS web interface.
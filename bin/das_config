#!/usr/bin/env bash

# setup environment
#. `dirname $0`/setup.sh

if  [ -z $DAS_PYTHONPATH ]; then
   echo "DAS_PYTHONPATH environment is not set"
   exit 1
fi

# run actual script
$DAS_PYTHONPATH/DAS/tools/create_das_config.py "$@"

#!/bin/sh

# setup environment
#. `dirname $0`/setup.sh

if  [ -z $DAS_PYTHONPATH ]; then
   echo "DAS_PYTHONPATH environment is not set"
   exit 1
fi

cmd=$DAS_PYTHONPATH/DAS/tools/das_mapping_db.py
$cmd ${1+"$@"}

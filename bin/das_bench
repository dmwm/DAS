#!/bin/sh

# setup environment
#. `dirname $0`/setup.sh

if  [ -z $DAS_PYTHONROOT ]; then
   echo "DAS_PYTHONROOT environment is not set"
   exit 1
fi

# run actual script
$DAS_PYTHONROOT/DAS/tools/das_bench.py ${1+"$@"}
